# دليل مرجعي — تطبيق رغوة (Laundry POS)

وثيقة تشرح كيف يشتغل كل جزء رئيسي بالتطبيق وظيفياً، بدون تفاصيل تقنية أو أكواد — لأي شخص يبي يفهم آلية عمل النظام.

**ملاحظة عامة:** كل شاشات التطبيق (الكاشير، العملاء، الموردين، التقارير، لوحة الأدمن) موجودة بملف واحد اسمه `App.jsx` — ما فيه ملفات منفصلة لكل صفحة.

**آخر تحديث لهذا الملف:** 2026-09-01. **لأي جلسة جديدة:** اكتب "اقرأ ملف PROJECT_REFERENCE.md وطبّق كل السياق منه" وبكون عارف كل شي مكتوب هنا مباشرة بدون ما تحتاج تشرح من جديد.

**⚠️ قسم 21 (واتس اب) جديد — أُضيف عمليًا بجلسة 2026-09-01 لكن لم يُتحقق حيًا بجلسة مصادقة حقيقية بعد (نفس القيد المعتاد). التحقق المطلوب منك مفصّل بآخر القسم.**

**⚠️ قسم 20 (تحقق PIN من السيرفر): ملف SQL اتشغّل فعليًا (2026-08-30، عبر اتصال Supabase المربوط بـComposio) وتحقق منه مباشرة — `owner_pin_credentials` موجود بـRLS مفعّل وصفر policies، وعمودي `tenant_settings` الجديدين موجودين بالقيم الافتراضية الصحيحة. لسا ما تم تحقق حي بجلسة مصادقة حقيقية كاملة (تعيين PIN فعلي وتجربة القفل) — نفس القيد المعتاد، يحتاج تجربتك أنت.**

**⚠️ اقرأ قسم 18 (دروس مستفادة) قبل أي تعديل على التوجيه (routing) أو `useEffect`/`useState` بملف `App.jsx` — فيه غلطتين حقيقيتين صارتا بجلسة 2026-08-26، إحداهما كسرت الموقع كامل لعدة دقائق.**

---

## 1) البحث عن العميل بشاشة البيع (POS)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `customers` (Supabase/Postgres) — كل صف فيه عمود `tenant_id` يربطه بالمحل

الكاشير يكتب بمربع بحث، والنظام يطابق فوراً (بدون ضغط زر) مع: اسم العميل (جزء من الاسم، بدون حساسية لحالة الأحرف)، أو رقم الجوال (جزء منه)، أو الرقم التعريفي للعميل. لو كتب علامة `#` قبل الأرقام، البحث يصير حصرياً بالرقم التعريفي فقط. النتائج تظهر بقائمة منسدلة حية أثناء الكتابة، والضغط على نتيجة يربط ذلك العميل بالفاتورة الحالية.

---

## 2) شاشة Active Invoices (الفواتير النشطة)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `invoices` (Supabase/Postgres)، مفلترة بعمود `tenant_id`

كل فاتورة تُنشأ من الكاشير تبدأ "مفتوحة" (غير مغلقة) وتظهر بهذي الشاشة تلقائياً، طالما إنها **مو** طلب توصيل. حالة الفاتورة المعروضة (استلام ← غسيل ← كوي ← جاهز ← تم التسليم) هي **أقل مرحلة وصلت لها أي قطعة بداخلها** — يعني لو قطعة وحدة لسا بمرحلة "غسيل" وباقي القطع "جاهزة"، الفاتورة كلها تظهر "غسيل". الفاتورة تختفي من هذي الشاشة فقط لما الموظف يضغط يدوياً زر "إغلاق الكل" من داخلها.

---

## 3) شاشة Active Delivery Invoices (فواتير التوصيل النشطة)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `invoices` (نفس مصدر الفواتير العادي)

نفس آلية شاشة "الفواتير النشطة" بالضبط، لكن تعرض فقط الفواتير اللي تم تحديدها كـ"طلب توصيل" وقت البيع (وفيها رسوم توصيل). فاتورة التوصيل تختفي من هذي القائمة بنفس الطريقة: بضغط "إغلاق الكل" يدوياً من داخلها.

---

## 4) سجل العملاء (Customer Ledger) ونظام المحفظة/الديون

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `customers` (الأرصدة الحالية) + جدول `customer_transactions` (سجل تاريخي لكل عملية شحن/سداد)

لكل عميل رصيدان منفصلان: **رصيد محفظة** (مبلغ مدفوع مسبقاً يقدر يصرفه لاحقاً) و**دين** (مبلغ مستحق عليه للمحل). شحن المحفظة يزيد الرصيد (ممكن مع خصم)، ولو دُفع بمبلغ حقيقي (نقد/شبكة) يُصدر له فاتورة/إيصال حقيقي بنفس اللحظة — عشان الإيراد يُحتسب مرة وحدة بس، مو مرتين لما يصرف من محفظته لاحقاً. سداد الدين الآن يمنع نهائياً إدخال مبلغ أكبر من المستحق فعلياً — النظام يرفض العملية ويعرض تحذير بدل ما يقبل أي مبلغ بصمت.

---

## 5) الموردين والمشتريات وحماية حد الدفعة

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `suppliers` (بيانات المورد ورصيده) + جدول `purchases` (كل عملية شراء)

كل مورد له رصيد يمثل المبلغ المستحق عليه للمحل (يزيد فقط لو الشراء كان "آجل"، ما يتأثر لو كان "نقد"). عند تسجيل سداد لهذا الرصيد، النظام يرفض أي مبلغ أكبر من المستحق فعلياً ويعرض رسالة تحذير واضحة، فما يصير رصيد المورد سالب أبداً بالغلط. كمان، ممكن إرفاق صورة أو ملف فاتورة المورد الأصلية مع كل عملية شراء للرجوع لها لاحقاً.

---

## 6) العروض والخصومات ومنع التداخل

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `promotions`

كل خصم له نوع (نسبة % أو مبلغ ثابت)، وإما يُطبّق تلقائياً أو يتطلب كوبون، وله فترة سريان اختيارية (بداية/نهاية). أي خصم موجود يمكن تعديله أو إلغاؤه (الإلغاء يعطّله فقط، ما يحذفه نهائياً). عند إضافة أو تعديل خصم، النظام يتحقق من كل خصم آخر لسا نشط (غير ملغى) ويرفض الحفظ لو فترته الزمنية تتقاطع مع خصم موجود، حتى ما يصير التباس أي خصم يُطبّق عند البيع.

---

## 7) التقارير (كل تبويب)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** مزيج من `invoices` + `purchases` + `expenses` + `customers` + `suppliers`

- **Sales Ledger (سجل المبيعات):** قائمة فواتير البيع قابلة للفلترة بتاريخ وطريقة دفع، مع تقسيم كل مبلغ لصافي + ضريبة + إجمالي.
- **Procurement Ledger (سجل المشتريات):** قائمة كل عمليات الشراء من الموردين مع إجمالي المصروف، وضريبة المدخلات المحتسبة فقط من الموردين اللي عندهم رقم ضريبي مسجّل.
- **Expenses (المصروفات):** قائمة مصاريف المحل العامة، كل مصروف عليه علامة "شامل الضريبة" أو "معفى"، قابلة للفلترة بتاريخ.
- **Profit & Loss (الربح والخسارة):** الإيرادات ناقص (المشتريات + المصروفات)، معروضة "منذ افتتاح المحل" وأيضاً لفترة محددة يختارها المستخدم، مع هامش الربح كنسبة.
- **Tax Return (الإقرار الضريبي):** ملخص ضريبة القيمة المضافة على طريقة هيئة الزكاة والضريبة (ربع سنوي أو شهري) — ضريبة المخرجات من المبيعات وضريبة المدخلات من المشتريات (فقط من الموردين المسجّلين ضريبياً)، وينتهي بصافي مبلغ مستحق للهيئة أو قابل للاسترداد، قابل للتصدير CSV أو الطباعة.

---

## 8) لوحة تحكم الأدمن

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جداول `registration_requests` + `tenants` + `sales_inquiries` + `platform_config`

- **طلبات التسجيل:** كل محل جديد يسجل حساب يُنشئ طلب ينتظر موافقة الأدمن (أو يُقبل تلقائياً حسب إعداد النظام)؛ الموافقة تُنشئ سجل المحل الفعلي.
- **إدارة بيانات المحلات:** يسمح للأدمن يشوف (قراءة فقط) فواتير/منتجات/مصاريف/مشتريات أي محل، ويتحكم بطرق الدفع المتاحة لذلك المحل.
- **التقارير المجمّعة:** أرقام تجمع بيانات كل المحلات مباشرة وبشكل حي — أفضل المحلات من ناحية الإيرادات، الأكثر نشاطاً بعدد الفواتير، نمو التسجيلات الجديدة بمرور الوقت، وتوزيع حالة سداد الفواتير عبر المنصة.
- **حذف محل:** يحذف نهائياً سجل المحل وكل ما تحته (منتجات، فواتير، عملاء، إلخ) وطلب التسجيل الأصلي المرتبط فيه، بعد نافذة تأكيد — عملية لا رجعة فيها.

---

## 9) نظام الفاتورة الضريبية المبسطة (QR)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** لا يُخزَّن بشكل منفصل — يُبنى وقت الطباعة من بيانات الفاتورة المكتملة نفسها

رمز QR (بصيغة هيئة الزكاة والضريبة السعودية) يظهر بالطباعة **فقط** لما تكون العملية بيع خاضع للضريبة فعلياً — يعني ما دُفع من محفظة العميل، والمحل عنده رقم ضريبي مسجّل بالإعدادات. غير كذا، تُطبع إيصال عادي بدون QR. الرمز يشفّر اسم المحل، الرقم الضريبي، تاريخ ووقت البيع، والإجمالي ومبلغ الضريبة — بنفس القيم المطبوعة بالضبط.

---

## 10) هيكل قاعدة البيانات العام (Supabase / Postgres)

النظام يستخدم Supabase (قاعدة بيانات Postgres حقيقية) — **مو Firestore**. لا يوجد تداخل "مستند ← مستندات فرعية"، كل الجداول مسطّحة (flat)، وكل جدول خاص ببيانات المحلات فيه عمود `tenant_id` يربط كل صف بمحله، مع سياسات Row Level Security (RLS) على مستوى قاعدة البيانات تمنع أي محل من قراءة أو تعديل صفوف محل ثاني.

- **`tenants`** — صف واحد لكل محل معتمد، ومعرّفه (`id`) هو نفسه حساب دخول صاحب المحل (Supabase Auth UID). الجداول التالية كلها فيها عمود `tenant_id` يشاور على هذا الصف:
  - `products`, `categories` — المنتجات وتصنيفاتها.
  - `invoices` — كل عملية بيع (وكل عملية شحن محفظة بمبلغ حقيقي).
  - `customers`, `customer_transactions` — بيانات العملاء وسجل محافظهم وديونهم.
  - `suppliers`, `purchases` — الموردين والمبالغ المدفوعة لهم.
  - `expense_categories`, `expenses` — المصاريف العامة.
  - `addons`, `service_types` — الإضافات الاختيارية وأنواع الخدمة.
  - `promotions` — الخصومات والعروض.
  - `tenant_settings` — بيانات المحل، كلمة مرور صاحب المحل (مخزّنة كـ hash)، طرق الدفع المفعّلة، لغة الواجهة.
- **`registration_requests`** — صف مستقل لكل طلب تسجيل محل جديد، مرتبط بحساب الدخول عبر عمود `uid` لكن **منفصل تماماً** عن `tenants` (حذف محل من `tenants` ما يحذف طلبه تلقائياً — لازم حذف الاثنين).
- **`sales_inquiries`** — رسائل نموذج "تواصل مع المبيعات" بصفحة الهبوط العامة، غير مرتبطة بأي محل.
- **`platform_config`** — جدول بصف واحد مشترك يحدد من هم الأدمنز (`admin_emails`)، وهل التسجيلات الجديدة تُقبل تلقائياً أو لأ (`auto_approve`).
- **`platform_admins`** — جدول صلاحيات المدير الفعلي (منفصل عن قائمة `admin_emails` النصية) — عضوية هذا الجدول هي اللي فعلياً تفعّل دالة `is_admin()` بقاعدة البيانات وتمنح وصول تجاوز RLS. `admin_emails` بـ`platform_config` مجرد قائمة "مسموحين يدخلوا لوحة الأدمن بالواجهة" — التحقق الحقيقي والحاسم يصير عبر `platform_admins` + `is_admin()`.
- **RLS (Row Level Security):** كل الـ21 جدول بقاعدة البيانات عليهم RLS مفعّل، وتم التأكد المباشر (بتاريخ 2026-08-23) إن كل سياسات `SELECT/INSERT/UPDATE/DELETE` على الجداول المرتبطة بمحل تتبع نمط `tenant_id = auth.uid()` بشكل صحيح — يعني ما فيه ثغرة IDOR حقيقية بقاعدة البيانات. كان فيه سياسات قديمة زايدة على 8 جداول (`categories, customers, expense_categories, expenses, invoices, products, purchases, tenant_settings`) تعطي وصول أدمن عبر بريد إلكتروني مكتوب مباشرة بالسياسة (`ragwapos@gmail.com`) بدل الاعتماد على `is_admin()`/`platform_admins` — تم حذفها (ملف `supabase-audit-drop-legacy-admin-policies.sql`) لأنها كانت تعني إن سحب صلاحية أدمن عبر الواجهة ما يسحبها فعلياً من هذي السياسات القديمة.

---

## 11) الهوية البصرية ونمط التصميم (Design System)

- **الخطوط:** كل التطبيق (مو بس صفحة الهبوط) يستخدم خط Cairo عبر كلاسات `.f-display` / `.f-body` / `.f-mono` (مُعرّفة بمكوّن `Fonts` بالكود).
- **ألوان التصميم (`tailwind.config.js`):** `brand-{50..900}` (تركوازي، اللون الأساسي)، `navy-{600..950}` (خلفيات داكنة، الشريط الجانبي)، `app-{bg,surface,border,border-strong,text,text-muted,text-subtle}` (رمادي فاتح للواجهة الداخلية)، `success/warning/danger/info-{50,500,600,700}` (ألوان الحالة)، ألوان `landing-*` منفصلة لصفحة الهبوط العامة فقط (خلفية فاتحة `#f6faf9`، مو نفس نظام الداخلية الداكن).
- **مكونات بصرية متكررة (مطبّقة بكل الشاشات الداخلية):**
  - **بطاقة KPI:** إطار دائري خفيف + أيقونة بمربع ملوّن + رقم كبير تحته.
  - **شارة حالة/تصنيف:** كبسولة بنقطة صغيرة، لون حسب الحالة (محايد/أساسي/معلومة/تحذير/نجاح/خطر).
  - **جدول:** إطار دائري، رأس الجدول بخلفية رمادية فاتحة، صفوف تتلوّن عند المرور بالماوس.
  - **رقاقات فلترة (Filter chips):** كبسولات، النشط منها تعبئة كاملة — أسود كحلي (`navy-900`) بشاشات الفواتير/المنتجات/التقارير، لكن تركوازي (`brand-500/600`) بفلاتر فئات المنتجات داخل نقطة البيع تحديداً (اختلاف مقصود، مو خطأ).
- **الشريط الجانبي:** العنصر النشط له تعبئة تركوازية شفافة + إطار داخلي رفيع + نقطة مؤشر، غير النشط نص رمادي فاتح فقط.
- **زر التبديل (Toggle) المشترك:** ثابت بالتصميم (`teal-600`/`stone-300`) — تم إصلاح بق فيه الدائرة الداخلية تطلع خارج حدود الزر بسبب اتجاه RTL (الحل: فرض `dir="ltr"` داخلياً على مسار الزر نفسه فقط).

---

## 12) نقطة البيع (POS) — تفاصيل الشاشة بعد آخر تعديل

- شريط "استلام من الفرع / توصيل" اللي كان أعلى الشاشة **انحذف بالكامل**. التوصيل الحين مربّع صغير داخل شبكة طرق الدفع نفسها، جنب "دفع متعدد" — تفعيله يفتح خانة سعر التوصيل تلقائياً، وما يأثر على اختيار طريقة الدفع الفعلية إطلاقاً (تبقى قابلة للاختيار بشكل طبيعي).
- خانة "كود الخصم" منقولة فوق، جنب مربع التوصيل مباشرة.
- طرق الدفع الحقيقية بنقطة البيع: نقدًا، شبكة خارجية، رصيد المحفظة، آجل (على الحساب) — بالإضافة لخيار خامس خاص بالكاشير فقط: "دفع متعدد" (Split)، وهذا الخيار الخامس **ما يظهر أبداً** كطريقة دفع قابلة للتفعيل/الإيقاف بشاشة الإعدادات (لأنه سلوك عملية بيع، مو طريقة دفع مستقلة).

---

## 13) الإعدادات — قسم "صلاحيات المالك فقط"

كرت "طرق الدفع المفعّلة" وكرت "إغلاق الأقسام عن الموظفين" ظاهرين دايماً مباشرة بالصفحة (مو مخفيين خلف بطاقة "افتح القسم" لازم تدخلها أول). الحماية الفعلية: أي محاولة تبديل زر جوه أي من الكرتين تطلب كلمة مرور المالك قبل ما تنطبق فعلياً — يعني الحماية موجودة على مستوى الفعل نفسه، مو على مستوى رؤية الكرت.

الأقسام الخمسة القابلة للقفل الفردي بكلمة مرور خاصة بها (`OWNER_SECTIONS`): سجل العملاء، المنتجات، المشتريات والمصروفات، العروض، التقارير. "طرق الدفع" **مو** قسم سادس قابل للقفل الفردي — هي محمية أصلاً بنفس كلمة مرور المالك الرئيسية اللي تحمي الكرتين كلهم، وتظهر بقائمة الأقسام كصف ثابت بعلامة "محمي" فقط للتناسق البصري.

---

## 14) الفوترة الإلكترونية (ZATCA Phase 2) — جاهزة تقنياً، معطّلة عمداً حتى يتوفر مفتاح حقيقي

**تحديث 2026-08-26:** الملفات الثلاثة نُقلت من `api/` إلى `coming-soon/zatca/` — يعني **ما عادت منشورة كـEndpoints حية على Vercel إطلاقاً** (أي ملف تحت `api/` ينشر تلقائياً، النقل خارجها يوقف ذلك). الكود نفسه محفوظ كامل بدون حذف (فيه `coming-soon/zatca/README.md` يشرح كيف يرجع لمكانه). كرت "الربط مع هيئة الزكاة والضريبة" بشاشة الإعدادات لسا ظاهر لصاحب المحل لكن بشارة **"قريبًا"** ثابتة بدل تدفق التفعيل الحقيقي — يتحكم فيه متغيّر واحد `ZATCA_FEATURE_LIVE` (حالياً `false`) قرب تعريف `ZatcaSettingsPanel` بـ`App.jsx`.

**السبب:** دوال `enableZatca`/`submitInvoiceToZatca` تسوي مكالمات حقيقية فعلية لواجهة هيئة الزكاة (API Compliance CSID + التوقيع) — **مو محاكاة** كما كان موثّق سابقاً بهذا الملف. بما إن المشروع ما عنده حساب/CSID مسجّل بعد بمنصة "فاتورة"، عرض تدفق تفعيل حي لمستخدمين حقيقيين كان بيرجّع أخطاء حقيقية من الهيئة أو يبدو متصل بدون ما يكون فعلاً.

- توليد مفتاح خاص (ECDSA secp256k1) وتشفيره (AES-GCM) قبل التخزين بجدول `zatca_configurations` — المفتاح المفكوك ما يطلع من السيرفر أبداً.
- بناء فاتورة UBL 2.1 (XML) تتضمن بيانات الفاتورة، توقيعها، وبناء رمز QR (Phase 2: 9 حقول، تشمل التوقيع والمفتاح العام) — كل القيم المدرجة بالـXML تمر عبر دالة `esc()` لمنع حقن XML.
- **للإرجاع لاحقاً:** انقل الملفات رجوع لـ`api/` (تفاصيل الخطوات بـ`coming-soon/zatca/README.md`)، وغيّر `ZATCA_FEATURE_LIVE` إلى `true`.
- **ملاحظة فحص أمني معروفة/مقبولة:** تنبيه Raqib عالي الخطورة (`raw-html-format`) على `coming-soon/zatca/zatca-sign-invoice.js:269` لا يزال يظهر بكل فحص رغم النقل والتصحيح (لف القيمة بـ`esc()`) — تأكدنا القيمة (base64) آمنة فعلياً وما تقدر تحتوي رموز XML خطرة؛ أداة الفحص الساكن (Semgrep) ببساطة ما تتعرف على `esc()` كدالة تعقيم وتعترض على أي إدراج مباشر بقالب XML بغض النظر عن المحتوى. هذا false positive معروف على كود حالياً غير منشور/غير مستخدم — قرار مقبول، مو خطأ يحتاج إصلاح إضافي إلا لو أعدنا هيكلة بناء XML بمكتبة حقيقية بدل القوالب النصية اليدوية.

---

## 15) الوضع الأمني الحالي (آخر تدقيق: 2026-08-24)

- **رؤوس الحماية:** `vercel.json` يضيف CSP، HSTS، X-Frame-Options: DENY، X-Content-Type-Options: nosniff، Referrer-Policy، Permissions-Policy على كل الاستجابات، بالإضافة لإعادة توجيه أي مسار غير معروف لـ`index.html` (حماية لأي رابط عميق مستقبلي).
- **صندوق تخزين الملفات:** الـbuckets `invoice-pdfs` و`tenant-documents` عليهم الحين حد فعلي من جهة السيرفر لحجم الملف (5 ميجا) ونوعه (صور + PDF فقط) — قبل كذا كان الفحص الوحيد بالمتصفح فقط (قابل للتلاعب).
- **شاشة خطأ:** التطبيق ملفوف بـ`Sentry.ErrorBoundary` — أي خطأ برمجي غير متوقع يعرض شاشة "حدث خطأ، أعد التحميل" بدل شاشة بيضاء فاضية.
- **فحص أسرار قبل كل commit:** ملف `.githooks/pre-commit` (مفعّل تلقائياً لأي نسخة من المشروع عبر `npm install` بفضل سكربت `prepare` بـ`package.json`) يفحص كل commit جديد عن أنماط أسرار حقيقية (مفاتيح خاصة، مفاتيح AWS/Stripe/Google، JWT خام، إلخ) ويرفض الـcommit لو لقى شي مشبوه.
- **إعدادات Cloudflare (نطاق ragwapos.com):**
  - DMARC: `p=quarantine` (كان `p=none`) — الخطوة القادمة (قرار بشري، مو تلقائي): الانتقال لـ`p=reject` بعد مراجعة تقارير الـquarantine.
  - أقل إصدار TLS مسموح: **1.2** (كان 1.0).
  - سجلات CAA مضافة صراحة: `pki.goog` (Google Trust Services، الشهادة الأساسية) و`letsencrypt.org` (Let's Encrypt، الشهادة الاحتياطية).
  - **DNSSEC مفعّل ونشط** (تأكيد Cloudflare: "Success! ragwapos.com is protected with DNSSEC").
- **فحص أمني خارجي (Raqib):** المشروع متصل بخدمة فحص أمني اسمها Raqib. تم إعداد Raqib كـMCP server باسم `raqib` (نطاق محلي/خاص بهذا المشروع فقط، يحتاج إعادة تشغيل الجلسة عشان تظهر أدواته `trigger_scan`/`get_report`) — الهدف منه تشغيل فحوصات أمنية مستقبلية مباشرة من جوه الجلسة بدل النسخ اليدوي للنتائج. **قاعدة ثابتة (قاعدة 5 بـCLAUDE.md):** فحص Raqib إلزامي قبل أي `git push` بدون استثناء، حتى بحالات الاستعجال (زي إصلاح عاجل لموقع معطّل) — التأخير المقبول (~30-70 ثانية) أهون من نشر شي بدون فحص.
- **مسار التقييم المرجعي (baseline) المقبول حالياً (77/100):** `api/zatca-sign-invoice.js:269` كان HIGH (صار مساره `coming-soon/zatca/zatca-sign-invoice.js:269` بعد نقل الملفات — نفس التنبيه، false positive موثّق بقسم 14)، `api/_cors.js:16` MEDIUM (راجعت الكود سطر بسطر بتاريخ 2026-08-27: الـ`Access-Control-Allow-Origin` يُرجَع فقط بعد تحقق `ALLOWED_ORIGINS.has(origin)` بتطابق نصي صارم لنطاقين محددين — لا wildcard ولا تحايل ممكن، وما فيه `Access-Control-Allow-Credentials` أصلاً. Semgrep على الأغلب ما يتتبع منطق الشرط قبل `setHeader`؛ **false positive مؤكد**)، رابط سياسة الخصوصية LOW (false negative — الرابط موجود فعلياً بصفحة الهبوط لكن الفحص ما يلقاه)، ومفتاح Firebase التاريخي بـ`src/firebase.js:4` LOW. أي تنبيه HIGH/CRITICAL **جديد** غير هذي الأربعة يستحق توقف فعلي وتحقيق، مو مجرد "نفس القائمة المعتادة".
- **بلوج `blog.ragwapos.com` أُلغي نهائياً (2026-08-30):** كان مستضاف بمشروع Netlify منفصل (`ragwa-blog.netlify.app`) عبر CNAME. تحقق مباشر (curl على `sitemap.xml` وكل رابط مقالة) أثبت إنه معطل وظيفياً بالكامل — 22 مقالة مذكورة بالـsitemap والصفحة الرئيسية، لكن كل رابط مقالة فعلي يرجع 404. تقرر إلغاؤه بدل إصلاحه، وحُذف سجل الـCNAME من Cloudflare بالكامل (لا يوجد أي مرجع له بالكود أصلاً — تحقق بالبحث الشامل بالمستودع قبل الحذف). هذا يزيل احتمال **Subdomain Takeover** المستقبلي (لو انحذف مشروع Netlify يوماً بدون حذف الـDNS أولاً، أي شخص كان يقدر يسجّل نفس اسم مشروع Netlify ويستولي على النطاق الفرعي). ملاحظة Raqib المعلوماتية "Subdomain Discovered: blog.ragwapos.com" من المتوقع تختفي من الفحوصات القادمة.
- **تحذير من نتائج فحص متذبذبة أثناء النشر:** لوحظ مرة (2026-08-26) إن فحص Raqib رجّع نتيجة سيئة جداً (رؤوس أمان CSP/HSTS/X-Frame-Options تظهر "مفقودة" تماماً، تقييم نزل من 77 لـ64) بينما `curl` مباشر على نفس اللحظة أكّد الرؤوس موجودة وصحيحة 100%. إعادة الفحص بعد دقائق رجّعت النتيجة الصحيحة (77، نظيف). السبب الأرجح: Raqib فحص بلحظة انتقالية أثناء نشر Vercel (deployment rollout) قبل ما تكتمل. **الدرس:** أي نتيجة فحص "كارثية" ومفاجئة تختلف جذرياً عن الفحص قبلها — تحقّق مباشرة بـ`curl` (headers فعلية، cache-busted) قبل ما تُصدّق النتيجة أو تاخذ أي قرار عليها، وأعد الفحص مرة ثانية للتأكيد.

---

## 16) ملخص تاريخي مختصر لمراحل العمل الأخيرة

1. **إعادة تصميم شاملة (Phases 1-4):** نقطة البيع، الفواتير، سجل العملاء، الإعدادات — نُشرت وأصبحت مباشرة على الموقع.
2. **مرحلة تصحيح دقيق (Strict pixel-correction):** تعديل 8 شاشات متوازي عبر وكلاء مستقلين (git worktrees) بمرجعية تصميم محددة بالحرف، دُمجت كلها بنجاح بفرع واحد بدون تعارضات متبقية.
3. **تنظيف كود ميت:** حذف أيقونات/متغيرات ترجمة غير مستخدمة، حذف إيموجي متبقي (استبدال بأيقونات Lucide حقيقية)، تأكيد تطابق 562 مفتاح ترجمة بالضبط بين العربي/الإنجليزي/الأردو بدون تكرار.
4. **تعديلات واجهة إضافية (بناءً على طلبات محددة):** إعادة تصميم قسم "صلاحيات المالك فقط" بالإعدادات، إعادة تنظيم شريط التوصيل بنقطة البيع، توزيع أعمدة جداول الفواتير بالتساوي، إصلاح موضع خانة البحث بالمنتجات، توحيد عرض خانة البحث بالمشتريات مع المصروفات، إصلاح بق زر التبديل.
5. **تدقيق أمني شامل (Security & Production-Readiness Audit):** فحص كامل — أسرار، مصادقة، RLS/IDOR، CORS، رؤوس الحماية، رفع الملفات — طُبّق كل إصلاح مُتحقق منه مباشرة (مو بس نظري)، شمل الاتصال المباشر بقاعدة بيانات Supabase الحية للتأكد من حالة RLS الفعلية بدل الافتراض.
6. **معالجة نتائج فحص Raqib (12 نتيجة):** تبيّن إن أغلب النتائج "الحرجة" كانت إنذارات كاذبة بعد التحقق الفعلي (مفاتيح عامة مصممة تكون ظاهرة، CORS محمي أصلاً بقائمة مسموحة)، لكن لقينا وأصلحنا ثغرة حقن XML حقيقية بملف توقيع فواتير ZATCA، وأضفنا رابط سياسة خصوصية، وشددنا كل إعدادات DNS/TLS بـCloudflare (DMARC/TLS/CAA/DNSSEC) بشكل حقيقي متحقق منه عبر استعلامات DNS عامة.
7. **جلسة موبايل + إصلاح اختيار العميل + ZATCA/404/روابط (2026-08-25/26):** تصحيح استجابة الموبايل بكامل الشاشات (جداول قابلة للتمرير، نقطة البيع)، إصلاح اختيار العميل من نتائج البحث بنقطة البيع (3 محاولات فاشلة قبل الوصول للسبب الحقيقي — تفاصيل بقسم 18)، تعطيل واجهة ZATCA مؤقتاً بشارة "قريبًا"، بناء صفحة 404 حقيقية، وإضافة روابط حقيقية لكل تبويبات لوحة التحكم — تفاصيل كاملة بقسم 17 والدروس المستفادة بقسم 18.
8. **مراجعة نتائج Raqib + إلغاء البلوج (2026-08-27/30):** تحقق مباشر بالكود أثبت `_cors.js:16` false positive مؤكد (مو بس "مقبول" افتراضياً — تفاصيل بقسم 15)؛ فحص `blog.ragwapos.com` الحي كشف إنه معطل وظيفياً بالكامل (كل روابط المقالات 404 رغم sitemap يعلن 22 مقالة)، فتقرر إلغاؤه نهائياً وحُذف سجل الـCNAME من Cloudflare بدل تركه معلّقاً (تفاصيل بقسم 15).
9. **إطلاق نظام `/guides` (2026-08-30):** دفعة أولى من 6 مقالات HTML ثابتة (مبنية وقت الـbuild، على نفس نطاق ونشر المشروع الحالي) لظهور رغوة بإجابات محركات الذكاء الاصطناعي — بديل آمن للبلوج المهجور، تفاصيل كاملة بقسم 19.

---

## 17) نظام الروابط الحقيقية (URL Routing) — الصفحات العامة ولوحة التحكم

### الصفحات العامة (قبل تسجيل الدخول)
`PATH_PAGE_MAP` بـ`App.jsx` يربط 7 مسارات ثابتة (`/`, `/en`, `/login`, `/signup`, `/forgot-password`, `/terms`, `/privacy`) بصفحاتها. أي مسار غير معروف يرجع `'notFound'` ويعرض `NotFoundPage` (كود HTTP **404 حقيقي**، مو 200 وهمي).

### لوحة التحكم (بعد تسجيل الدخول)
كل تبويب رئيسي له رابط حقيقي: `/home`, `/pos`, `/active-invoices`, `/delivery-invoices`, `/customers`, `/inventory`, `/purchases`, `/promotions`, `/reports`, `/settings`. تبويبات "التقارير" و"المشتريات" الفرعية لها روابط فرعية كمان (مثلاً `/reports/vat`, `/purchases/expenses`). كل هذي المسارات `noindex,nofollow` (خلف تسجيل دخول، ما فيها فايدة فهرسة).

- **`navigateTab(tab, subTab)`** (داخل `LaundryOpsApp`): يحدّث الرابط (`pushState`) والحالة معاً — كل أزرار التنقل الداخلية (الشريط الجانبي، أزرار "إجراءات سريعة" بالرئيسية، تبديل التبويبات الفرعية) تمر منه.
- **`requestTab(key, subTab)`**: نفس `navigateTab` لكن يتحقق أول من `sectionLocks` — لو القسم مقفول بـPIN، يفتح طلب PIN بدل التنقل المباشر (نفس التدفق سواء كان الضغط من الشريط الجانبي أو رابط مباشر/deep link).
- **زائر بدون جلسة على رابط لوحة تحكم** → يتحول تلقائياً لـ`/login` (داخل `onAuthStateChange`، مو بـ`middleware.js` — الأخير ما يقدر يميّز حالة الدخول بدون تعقيد إضافي بفحص كوكي Supabase على الـEdge runtime).
- **`middleware.js`** (جذر المشروع، Vercel Routing Middleware): يرجع 404 حقيقي لأي مسار مو بالقائمة المعروفة (`KNOWN_PATHS`)، ويخدم الباقي بشكل طبيعي (200). **لازم يبقى متزامن يدوياً** مع `PATH_PAGE_MAP`/`DASHBOARD_TAB_PATHS`/`REPORTS_SUBTAB_PATHS`/`PURCHASES_SUBTAB_PATHS` بـ`App.jsx` — ما فيه استيراد آلي بينهم (Edge runtime منفصل).
- **سبب اختيار `middleware.js` بدل `vercel.json`'s legacy `routes`:** الأخير يدعم `status: 404` مباشرة بس **ما يقدر يتعايش مع `headers`** بنفس الملف — وملف المشروع فيه رؤوس أمان حرجة (CSP إلخ) ما نبي نلمسها بتغيير كبير.

---

## 18) دروس مستفادة (2026-08-26) — اقرأها قبل أي تعديل مشابه

### 1. علة اختيار العميل بنقطة البيع (Android/Samsung Internet) — 4 محاولات
المستخدم بلّغ إن اختيار عميل من نتائج البحث بنقطة البيع ما يشتغل بالموبايل. **3 محاولات إصلاح فشلت** (تغيير حدث "الإغلاق عند الضغط خارج الصندوق" من `pointerdown` → `mousedown` → `mousedown`+`touchstart`) لأن الفرضية كانت غلط من الأساس. الحل الفعلي طلع بعد إضافة **سطر تشخيص مؤقت يظهر على الشاشة نفسها** (بدون أدوات مطورين) كشف إن الضغطة فعلاً كانت تختار العميل، لكن ضغطة "شبح" مكررة (Android ghost-click، علة معروفة) توصل بعدها مباشرة على نفس الإحداثيات وتضرب زر الإلغاء بالبطاقة الجديدة — كل هذا بنفس اللحظة بدون أي وميض مرئي. **الحل الصحيح:** معالجة الاختيار عبر `onTouchEnd` + `preventDefault()` (يمنع المتصفح من توليد أي حدث مكرر لنفس اللمسة من الأساس)، مو محاولة تخمين "وين بتضرب" الضغطة المكررة.

**الدرس:** بعد فشل 2 محاولة إصلاح مبنية على تخمين/نظرية، **توقف عن التخمين وأضف تشخيص مباشر** (سطر نص على الشاشة، حتى لو مؤقت وبسيط) بدل محاولة ثالثة رابعة بدون بيانات حقيقية من الجهاز.

### 2. خلل TDZ كسر الموقع كامل بالإنتاج لعدة دقائق
أثناء إصلاح خلل بـ`popstate` (سطر الرجوع بالمتصفح كان يطيح المستخدم المسجّل دخوله على الصفحة العامة)، أضفنا `tenantId` لمصفوفة اعتماديات `useEffect` — لكن هذا الـ`useEffect` معرّف **قبل** سطر `const [tenantId, setTenantId] = useState(null)` بترتيب الكود داخل نفس الدالة. مصفوفة الاعتماديات تُقيَّم **فوراً وقت الرندر** (بعكس جسم الـeffect نفسه اللي يشتغل لاحقاً)، فأي إشارة لمتغير `const` قبل سطر تعريفه تطلع خطأ `ReferenceError: Cannot access 'x' before initialization` — بهذي الحالة على **كل رندر**، يعني الموقع كامل يطلع شاشة "حدث خطأ غير متوقع" (Sentry ErrorBoundary) لكل زائر، مسجّل دخول أو لا.

**الدرس:**
- قبل إضافة أي متغيّر لمصفوفة اعتماديات `useEffect`/`useMemo`/`useCallback` بملف `App.jsx` (فيه دوال ضخمة بترتيب حالات/effects غير مجمّع بالأعلى)، تأكد سطر تعريف المتغيّر (`useState`/`const`) يسبق الـeffect فعلياً بترتيب الكود.
- `npm run build` **ما يكشف هذا النوع من الأخطاء** — خطأ وقت التشغيل (runtime)، مو خطأ بناء. لازم فحص فعلي بمتصفح حقيقي (حتى بدون جلسة، كافي محلياً) والتأكد من عدم وجود أخطاء بالـconsole قبل أي نشر يمس ترتيب الـhooks.
- لو صار كسر مشابه بالإنتاج مرة ثانية: أعد إنتاج الخطأ بسرعة على **السيرفر المحلي غير المصغّر** (`npm run dev`) — رسالة الخطأ بالنسخة المصغّرة بالإنتاج تُظهر اسم متغيّر مشوّه بحرف واحد (مثلاً `'m'`)، بينما النسخة المحلية تُظهر الاسم والسطر الحقيقيين فوراً.

### 3. تحقق دايماً بمتصفح حقيقي (بجلسة مسجّلة) بعد أي تغيير على التوجيه
حتى بعد نشر كل إصلاح والتأكد إنه اجتاز فحص Raqib و`curl`، **لقينا 3 أخطاء حقيقية إضافية بالتوجيه ما كان ممكن نلقاها إلا باختبار فعلي بمتصفح فيه جلسة دخول حقيقية**: (أ) الرجوع بالمتصفح من تبويب داخلي كان يطيح المستخدم على الصفحة العامة (لأن مستمع popstate القديم للصفحات العامة ما كان يعرف بوجود جلسة نشطة)، (ب) نفس هذا الإصلاح هو اللي سبب خلل TDZ بالنقطة 2، (ج) الرجوع لرابط `/` الأصلي كان يترك محتوى تبويب قديم معلّق لأن `/` نفسها ما كانت معروفة كـ"الرئيسية". `curl` وفحص الرؤوس **ما يكفي أبداً** لتغييرات تمس React state/routing — لازم متصفح حقيقي ينفّذ الجافاسكربت فعلياً.

---

## 19) صفحات `/guides` — محتوى ثابت لظهور رغوة بإجابات محركات الذكاء الاصطناعي (2026-08-30)

**السياق:** بعد إلغاء بلوج `blog.ragwapos.com` المكسور (قسم 15)، اتفقنا إن أي محتوى نبيه يظهر بإجابات الذكاء الاصطناعي لازم يكون HTML خام كامل بأول استجابة سيرفر — لأن أغلب زواحف الذكاء الاصطناعي (GPTBot, ClaudeBot, PerplexityBot, CCBot) **لا تُشغّل جافاسكربت**، بعكس بقية هذا التطبيق (SPA يُرندر كل شي عبر React، `index.html` فيه `<div id="root"></div>` فاضي فقط).

**كيف يشتغل:**
- `content/guides.mjs`: بيانات كل مقالة (عنوان، وصف، فقرات نصية عادية بدون HTML خام بالبيانات نفسها).
- `scripts/build-guides.mjs`: سكربت Node بسيط (صفر تبعيات npm جديدة) يولّد `public/guides/<slug>.html` لكل مقالة + `public/guides/index.html`، مع `<title>`/meta/OG/JSON-LD (`Article` schema) حقيقيين بكل ملف — **يُستدعى تلقائيًا كخطوة `prebuild`** بـ`package.json` قبل `vite build`، يعني أي `npm run build` (محلي أو على Vercel) يعيد توليدها دايمًا.
- **`public/guides/` مُستثنى من git (`.gitignore`)** — هذا مخرجات بناء (build artifact) بالضبط زي `dist/`، مو مصدر. المصدر الوحيد اللي يُراجع/يُعدَّل هو `content/guides.mjs`.
- **التوجيه:** `middleware.js` يمرّر أي مسار يبدأ بـ`/guides` بدل معاملته كمسار مجهول (404). `vercel.json` فيه `rewrites` صريحة (`/guides` → `/guides/index.html`، `/guides/:slug` → `/guides/:slug.html`) **قبل** قاعدة الـSPA العامة (ترتيب المصفوفة يهم).
- تصميم كل صفحة self-contained (`<style>` inline، مو معتمد على ملف CSS المبني بواسطة Vite لأن اسمه مرتبط بـhash يتغيّر كل build) — بالوان/خط نفس نمط الصفحة العامة الحالية (خلفية `landing.bg`، خط Cairo، تذييل navy).
- رابط تذييل واحد من الصفحة الرئيسية العامة (`src/App.jsx`, footer) لـ`/guides` — **فقط بالصفحة العامة قبل تسجيل الدخول**، ما يظهر لأي عميل داخل لوحة التحكم.
- `public/sitemap.xml` يشمل `/guides` وكل مقالة.

**لإضافة مقالة جديدة مستقبلاً:** ضيف عنصر جديد لمصفوفة `guides` بـ`content/guides.mjs` (نفس البنية: `slug`, `title`, `description`, `datePublished`, `sections`)، أضف رابطها بـ`public/sitemap.xml`، وشغّل `npm run build` — السكربت يتكفل بالباقي تلقائيًا. **لا تعدّل `public/guides/*.html` يدويًا مباشرة** (مخرجات بناء، تُمحى وتُعاد كتابتها بالكامل بكل build).

**لماذا هذا التصميم أأمن من البلوج المهجور:** صفر استضافة/حساب/نطاق فرعي منفصل، صفر endpoints جديدة، صفر اتصال بقاعدة بيانات، محتوى ثابت 100% وقت الـbuild (يلغي فئة كاملة من ثغرات الحقن)، يرث رؤوس الحماية الحالية تلقائيًا (`vercel.json` يطبّقها على كل المسارات)، ومغطى تلقائيًا بفحص Raqib الإلزامي (قاعدة 5) من أول commit — بعكس Netlify المنفصل اللي كان خارج أي مراقبة.

---

## 20) تحقق PIN المالك/الأقسام من السيرفر (2026-08-31) — إصلاح تدقيق أمني (§4.1)

**المشكلة الأصلية:** `ownerPassword` (كلمة مرور المالك الرئيسية) وقيم `sectionLocks` الخمسة (customers/inventory/purchases/promotions/reports) كانت تُخزَّن كـ SHA-256 **بدون ملح** وتُقارَن `===` **بالمتصفح نفسه** — والقيمة المُشفّرة كانت تُرسَل كاملة لأي جلسة مسجّلة لنفس المستأجر عبر صف `tenant_settings` (نفس الجلسة اللي يشترك فيها المالك والموظفين على نفس الجهاز/الحساب). يعني أي شخص يعرف يفتح Network tab أو React DevTools يقدر يستخرج الهاش ويكسره offline بسهولة (مساحة PIN من 4 أرقام = 10,000 احتمال بس).

**الحل:** تحقق كامل بالسيرفر عبر **`api/verify-pin.js`** (endpoint جديد) — الهاش/الملح ما يطلعون للعميل تحت أي ظرف، الاستجابة دايمًا `{success, error?}` بس.

- **`supabase-owner-pin-hardening.sql`** (يُشغَّل يدويًا مرة وحدة بـSupabase SQL Editor): جدول جديد `owner_pin_credentials` (tenant_id+scope PK، `pin_hash`/`pin_salt` بصيغة scrypt، `failed_attempts`/`locked_until` لتحديد معدل المحاولات) — RLS مفعّل بدون أي policy لـ`anon`/`authenticated` (نفس نمط `platform_admins`)، وصول حصري عبر مفتاح service-role من `api/verify-pin.js`. بالإضافة لعمودين خفيفين جدد على `tenant_settings`: `owner_pin_set` (boolean) و`locked_sections` (jsonb array من مفاتيح الأقسام المقفولة) — **معلومة غير حساسة إطلاقًا** (بس "مقفول ولا لأ"، مو قيمة القفل نفسها)، يقرأها العميل عبر نفس اشتراك `subscribeToRow` الموجود أصلاً.
- **`api/verify-pin.js`**: نمط تحقق جلسة مطابق لـ`admin-sync.js` (`supabaseAdmin.auth.getUser(token)` → `tenantId = caller.id`)، + تحديد معدل عبر `_rateLimit.js`/Upstash الموجودة فعليًا (5 محاولات/دقيقة للتحقق، 10/دقيقة لعمليات الإدارة). أربع عمليات: `verify` (تحقق PIN لأي scope)، `set_master` (إنشاء أول مرة فقط)، `set_section`/`disable_section` (تتطلب إرسال `masterPin` **بنفس الطلب** ويُعاد التحقق منه بالسيرفر — سد ثغرة كانت موجودة بالتصميم الأولي للخطة: لولا هذا، أي جلسة تقدر تستدعي `set_section` مباشرة بدون معرفة الـmaster PIN أصلاً، لأن التسلسل بالواجهة كان مجرد ترتيب أزرار، مو تحقق فعلي).
- **الترحيل التلقائي الصامت:** `verifyScope()` تتحقق أول من `owner_pin_credentials`؛ لو الصف غير موجود، ترجع للقيمة القديمة بـ`tenant_settings` (`owner_password`/`section_locks[scope]`) وتقارنها بـSHA-256 (نفس المنطق القديم، بس بالسيرفر). لو صحّت: تشتق هاش `scrypt` جديد بملح عشوائي فورًا، تكتبه بالجدول الجديد، وتمسح القيمة القديمة — بدون أي إزعاج للمستخدم، بنفس فلسفة `// legacy plaintext — self-heal to a hash` الموجودة أصلاً بالكود قبل هذا الإصلاح.
- **`src/App.jsx`:** حُذفت `sha256Hex` بالكامل (كانت تُستخدم بـ5 أماكن فقط، كلها استُبدلت). `ownerPassword`/`setOwnerPassword` (state بـ`LaundryOpsApp`) استُبدلت بـ`ownerPinSet`/`setOwnerPinSet` (boolean بس). `sectionLocks` بقيت بنفس الاسم لكن قيمها الآن boolean حقيقي (مو هاش) مُشتقّة من `lockedSections` array. `callVerifyPin()` (دالة مساعدة قرب `PinPromptModal`) هي نقطة الاتصال الوحيدة بـ`/api/verify-pin` — تُستدعى من `OwnerOnlySettings` (تعيين/تفعيل PIN المالك والأقسام) وPIN الدخول عند رابط قسم مقفول مباشر (`AppShell`).
- **عقد `PinPromptModal` (2026-08-31):** `verify`+`onSuccess` (fire-and-forget) استُبدلا بـ`onSubmitPin(pin) -> Promise<boolean>` (يسوي العملية الفعلية عبر `callVerifyPin`) + `onDone(pin)` (sync، يُستدعى **بعد** أنيميشن النجاح ~450ms، هنا الأب يسوي انتقال الحالة المحلي). المكوّن نفسه يدير حالة `phase` (idle/busy/success/error) لعرض تحميل حقيقي (`Loader2 animate-spin` + "جارِ الحفظ...")، أيقونة قفل/فتح تتحول عند النجاح، واهتزاز (`@keyframes pin-shake` بـ`src/index.css`، بنفس نمط `.scrollbar-none`) عند الخطأ — **صفر مكتبة أنيميشن جديدة** (Tailwind transitions + lucide icons فقط، بنفس الأسلوب المستخدم بكل التطبيق). مكوّن `Modal` العام **لم يُلمس** — الأنيميشن محصور داخل `PinPromptModal` نفسه فقط. أي كود يستدعي `PinPromptModal` مستقبلاً يحتاج يستخدم `onSubmitPin`/`onDone`، مو `verify`/`onSuccess` القديمين.
- **تحسين أداء (2026-08-31):** مسار الترحيل بـ`api/verify-pin.js` كان يسوي حتى 7-10 رحلة قاعدة بيانات متتالية (قراءة/كتابة منفصلة لكل من مسح القيمة القديمة ومزامنة الأعلام). دُمجت بدالة `finalizeTenantSettings()` وحيدة (تعيد استخدام صف `tenant_settings` المقروء أصلاً بدل قراءته مرتين) — قلّلت العدد لحوالي النص بدون أي تغيير بقوة التشفير أو تحديد المعدل أو RLS.
- **`tenant_settings.owner_password`/`section_locks` القديمين:** ما زالوا موجودين مؤقتًا (يُقرأون من السيرفر فقط أثناء الترحيل) — حذفهم النهائي خطوة تنظيف منفصلة لاحقة بعد التأكد من ترحيل كل المستأجرين النشطين (قرار مستقبلي، مو جزء من هذا الإصلاح).

**✅ ملف الـSQL اتشغّل فعليًا** (2026-08-30، عبر اتصال Supabase المربوط بـComposio — بديل عن تشغيله يدويًا بـSupabase SQL Editor). تحقق مباشر بعد التشغيل: `owner_pin_credentials` موجود (`relrowsecurity = true`، `policy_count = 0`)، و`tenant_settings.owner_pin_set`/`locked_sections` موجودين بالقيم الافتراضية الصحيحة. فحص سريع لـ`/api/verify-pin` بدون token يرجع `401 missing_token` كما متوقع (مو خطأ 500 — يعني الجدول موجود والكود يشتغل صح لين نقطة التحقق من الجلسة).

**⚠️ لم يُتحقق حيًا بجلسة مصادقة حقيقية بعد** (نفس القيد المتكرر بهذا المشروع — لا أدخل كلمات مرور حتى لو أُعطيت). تحققت فقط: `npm run build` ناجح، فحص متصفح حي بدون أخطاء console على الصفحة العامة (الجزء المصادق ما قدرت أوصله)، ومراجعة كود شاملة (تطابق أسماء الـprops بين كل المكوّنات، صفر مراجع متبقية لـ`ownerPassword`/`sha256Hex`). **التحقق المطلوب منك بعد تشغيل ملف الـSQL:** فتح Settings → تفعيل قفل قسم → التأكد يطلب PIN المالك ثم PIN جديد للقسم → تسجيل خروج/دخول والتأكد التحقق يشتغل صح من رابط القسم المقفول مباشرة.

**🐛 خلل حقيقي انلقى ووانصلح بأول تجربة فعلية (2026-08-31):** مستأجر عنده أصلاً `owner_password`/`section_locks.promotions` مُعبّين من *قبل* هذا الإصلاح (استخدم ميزة قفل الأقسام بالنظام القديم غير الآمن سابقًا) جرّب يقفل قسم — الواجهة عرضت "أنشئ باسورد مالك جديد" بدل "أدخل الباسورد الموجود" (لأن `owner_pin_set` يبدأ `false` افتراضيًا بالعمود الجديد، بغض النظر عن وجود قيمة قديمة)، فلما جرب ينشئ باسورد "جديد"، السيرفر صح رفضه (`409 already_set`، لأنه فعليًا يكتشف الـlegacy hash الموجود) فطلعت رسالة "تعذّر إتمام العملية". **السبب:** الترحيل التلقائي (lazy migration on verify) صحيح تصميميًا، لكن ناقصه **تصحيح أولي (backfill) لعلم `owner_pin_set`/`locked_sections` نفسه** — العلم ما كان يعكس وجود بيانات قديمة أصلاً. **الإصلاح:** استعلام UPDATE واحد (مضاف بآخر `supabase-owner-pin-hardening.sql`) يحسب العلمين من الأعمدة القديمة مباشرة، طُبّق فورًا عبر اتصال Composio-Supabase وتحقق منه (تحوّل مستأجر واحد فعليًا من `false`/`[]` لـ`true`/`["promotions"]`، الاثنين الباقيين بلا بيانات قديمة بقوا `false`/`[]` بشكل صحيح). **الدرس:** أي عمود جديد بقيمة افتراضية "فاضية" يعتمد عليه منطق عرض بالواجهة (enter مقابل set) يحتاج تصحيح أولي صريح لبيانات موجودة مسبقًا، مو بس الاعتماد على الترحيل الكسول وقت الاستخدام الفعلي — الاثنين مطلوبين معًا.

**🐛 خلل ثانٍ أخطر — كسر حفظ الإعدادات بالكامل (2026-08-31):** لما حذفنا `sectionLocks`/`ownerPassword` من upsert العميل لـ`tenant_settings` (قسم 4 أعلاه)، ما انتبهنا إن عمود `section_locks` نفسه **NOT NULL بدون قيمة افتراضية** بقاعدة البيانات (بعكس `owner_password` اللي كان أصلاً nullable). النتيجة: `INSERT ... ON CONFLICT DO UPDATE` (upsert) يفشل بـ400 **حتى على صف موجود فعلاً** — لأن Postgres يبني صف المرشح الكامل ويتحقق من قيود NOT NULL قبل ما يوصل لمرحلة تعارض/تحديث، بغض النظر إن التحديث نفسه ما كان بيلمس هذا العمود. يعني **أي حفظ إعدادات (اسم المحل، اللغة، واتساب، طرق الدفع) صار يفشل بصمت** لكل المستأجرين، مو بس مسار الـPIN. **الإصلاح:** `alter table tenant_settings alter column section_locks set default '{}'::jsonb;` (تحقق أولاً القيم الحالية بكل الصفوف عشان نتأكد الإضافة ما تلمس بيانات موجودة — الافتراضي يُستخدم فقط وقت الإدراج، ما يغيّر قيمة صف موجود). **الدرس:** عند حذف عمود من upsert شامل، لازم تتحقق من nullability/default كل عمود متبقي بنفس الجدول، مو بس الأعمدة اللي حذفتها فعليًا — Postgres يتحقق من الصف كامل وقت upsert.

**🐛 خلل ثالث (CSP، مو من هذا الإصلاح لكن انلقى بنفس الجلسة):** رؤوس CSP بـ`vercel.json` كانت تحظر بيكون Cloudflare Web Analytics (`static.cloudflareinsights.com` — ميزة تحليلات مفعّلة فعليًا لهذا الموقع بلوحة Cloudflare) وworker الخاص بـSentry Replay (`replayIntegration()` مفعّل بـ`src/main.jsx`، `replaysOnErrorSampleRate: 1.0` — يسجّل جلسة إعادة تشغيل عند أي خطأ، بالضبط سيناريو الخلل أعلاه). أضفنا `https://static.cloudflareinsights.com` لـ`script-src`، `https://*.cloudflareinsights.com` لـ`connect-src`، و`worker-src 'self' blob:` صريح (كان يعتمد على fallback من script-src اللي ما يسمح بـblob:).

---

## 21) قسم "واتس اب" — مركز رسائل واتساب (2026-09-01)

**السياق:** قبل هذا التعديل، إعداد رسالة الفاتورة (تفعيل/نص) كان بطاقة واحدة داخل "الإعدادات" (`WhatsAppSettingsPanel`، محذوفة الآن بالكامل)، وما فيه أي رسالة تلقائية عند وصول الطلب لحالة "جاهز". صار عنصر مستقل بالشريط الجانبي اسمه "واتس اب" (بين "التقارير" و"الإعدادات")، بصفحة `/whatsapp` خاصة به، مركز واحد لكل إعدادات رسائل واتساب.

- **البطاقتان بقسم "الرسائل":** مكوّن مشترك `MessageTemplateCard` (بديل `WhatsAppSettingsPanel`) لكل من "إرسال الفاتورة" (نفس `whatsappEnabled`/`whatsappTemplate` القديمين تمامًا، فقط نُقلت واجهتهما — صفر بيانات ضائعة) و"الطلب جاهز" (حقول جديدة). كل بطاقة Compact افتراضيًا (Toggle + Preview مختصر بتلوين `{tags}`) وتتوسع بزر "تعديل الرسالة" لمحرر (textarea + رقاقات متغيرات قابلة للإدراج بموضع المؤشر) — التعديل محلي حتى الضغط على "حفظ الرسالة"، و"إلغاء"/طي المحرر يتجاهل أي تعديل غير محفوظ.
- **قسم "سلوك الإرسال":** ثلاث بطاقات Radio (`whatsappReadySendMode`): `auto` (فتح واتساب فورًا بدون أي نافذة)، `ask` (نافذة تأكيد `WhatsAppReadyConfirmModal` — Preview + زرّي "إرسال عبر واتساب"/"تخطي" + اختصارات `Enter`/`Esc`)، `off` (لا شي إطلاقًا). منفصل تمامًا عن Toggle تفعيل رسالة "الطلب جاهز" نفسها (`readyMessageEnabled`) — لازم الاثنان معًا (مفعّلة + غير `off`) عشان أي إرسال يصير.
- **نقطة التحويل الفعلية:** `updateItemStatus` بـ`InvoicesView` (تُستخدم من شاشتي الفواتير النشطة وفواتير التوصيل معًا، وأيضًا من `InvoiceDetailModal`) — دالة `maybeNotifyReady` تقارن `invoiceOverallStatus` **قبل وبعد** التحديث، وتُطلق الرسالة **فقط** لحظة العبور مِن غير-جاهز إلى جاهز (مو كل تعديل بينما الفاتورة أصلًا جاهزة). تغيير حالة الطلب نفسه يُحفظ دائمًا بشكل مستقل — الإرسال أو تخطيه لا يؤثر عليه أبدًا.
- **عميل بدون رقم جوال:** لو ما فيه `mobile` مسجّل، الدالة ترجع فورًا بصمت — بدون نافذة، بدون محاولة فتح واتساب، بدون أي خطأ.
- **إعادة استخدام كاملة، صفر نظام إرسال جديد:** نفس `openWhatsApp()`/`cleanPhoneForWhatsApp()` الموجودين أصلًا (لم يُلمَسا)، ونفس `fillWhatsAppTemplate()` — أُضيف لها بارامتر ثالث اختياري `fallback` (افتراضيًا `DEFAULT_WHATSAPP_TEMPLATE` كما كان) عشان رسالة "الطلب جاهز" تستخدم افتراضيها الخاص (`DEFAULT_READY_MESSAGE_TEMPLATE`، بـ`src/utils/phone.js`) بنفس الدالة بدون كسر أي نداء قديم. مسار إرسال الفاتورة نفسه (Popup الفاتورة → "إرسال عبر واتساب" → `shareOnWhatsApp`) **لم يتغيّر إطلاقًا**.
- **قاعدة البيانات:** ثلاثة أعمدة جديدة بـ`tenant_settings` (`supabase-whatsapp-ready-message.sql`، طُبّقت فعليًا 2026-09-01 عبر اتصال Composio-Supabase، متحقق منها مباشرة): `ready_message_enabled boolean NOT NULL DEFAULT false`، `ready_message_template text` (nullable، نفس نمط `whatsapp_template` — فاضي يعني "استخدم الافتراضي")، `whatsapp_ready_send_mode text NOT NULL DEFAULT 'ask'` (بـCHECK محصور بـ`auto`/`ask`/`off`). **كل عمود جديد له DEFAULT عمدًا** — تفاديًا لنفس فئة الخلل الموثّقة أعلاه (§20، "خلل ثانٍ أخطر": عمود NOT NULL بدون DEFAULT كسر upsert الإعدادات كامل). الحقول الثلاثة أُضيفت لنفس upsert effect الموحّد بـ`LaundryOpsApp` اللي يحفظ كل حقول الإعدادات معًا دائمًا (نفس نمط "merge:true" الموثّق سابقًا).
- **التوجيه:** `/whatsapp` أُضيف لـ`DASHBOARD_TAB_PATHS`/`NAV` بـ`App.jsx` **و**`KNOWN_PATHS` بـ`middleware.js` معًا (نفس قيد التزامن اليدوي الموثّق بقسم 17). لا قفل PIN على هذا التبويب (لم يُطلب).
- **مرجع التصميم:** الصفحة صُممت لتطابق مرجع بصري فُحص فعليًا خلال الجلسة (`localhost:8934/whatsapp.html`) — تبيّن إنه أصلًا مبني بنفس توكنز رغوة (`app-surface`/`app-border`/`brand-500/600`/بطاقة KPI بأيقونة مربعة ملوّنة من §11)، فالتطبيق هنا استخدم نفس الكلاسات الحرفية الموجودة بالمشروع، صفر مكتبة تصميم/أنيميشن جديدة (Tailwind + lucide-react فقط، بنفس أسلوب `PinPromptModal`).
- **لم يُبنَ عمدًا:** زر "معاينة" مستقل لفتح نافذة "الطلب جاهز" بدون طلب حقيقي (كان موجود بالمرجع كأداة عرض للموك أب الثابت فقط، خارج نطاق الطلب الفعلي).

**✅ `npm run build` ناجح، وفحص متصفح حي للصفحة العامة صفر أخطاء console.** ⚠️ **لم يُتحقق حيًا بجلسة مصادقة حقيقية بعد** (نفس القيد المتكرر — لا أدخل كلمات مرور حتى لو أُعطيت). **التحقق المطلوب منك:** فتح "واتس اب" من الشريط الجانبي والتأكد إعداد رسالة الفاتورة انتقل ببياناته القديمة سليمة؛ تجربة تعديل/حفظ الرسالتين (Compact↔Expand)؛ تجربة الأوضاع الثلاثة لسلوك الإرسال على طلب حقيقي (تلقائي يفتح واتساب مباشرة بدون نافذة، سؤال يفتح نافذة التأكيد مع اختبار `Enter`/`Esc`، عدم الإرسال بدون أي أثر)؛ وتجربة عميل بدون رقم جوال (يجب المرور بصمت بدون أي نافذة أو خطأ).

**🐛 بلاغان بعد النشر، أُصلحا بنفس الجلسة (2026-09-01):**
1. **ترتيب نتائج بحث العميل بشاشة البيع مو صحيح** (مثال المستخدم: العميل #2 يطلع بعد #4). السبب: `CustomerPicker` (نقطة البيع فقط) يبني نتائج البحث بـ`.filter()` بدون أي `.sort()`، ومصدر `customers` نفسه (`subscribeToTable` بـ`LaundryOpsApp`) يُستدعى بدون الباراميتر الاختياري `orderColumn` أصلًا — فترتيب الجلب الأولي غير مضمون، وأي عميل جديد يُلحق بآخر المصفوفة بغض النظر عن رقمه. **الإصلاح:** `.sort((a, b) => a.id - b.id)` على نتائج `CustomerPicker` المفلترة وقت العرض (إصلاح محصور بمكوّن البحث، رخيص لأنه على النتائج القليلة المفلترة فقط).
2. **تذبذب/قفز أزرار "سلوك الإرسال" بصفحة واتس اب عند التنقل بينها بسرعة.** التشخيص (مبني على قراءة كود، **لم يُتحقق منه حيًا** — نفس القيد المعتاد): useEffect الحفظ التلقائي لـ`tenant_settings` بـ`LaundryOpsApp` كان يكتب upsert فورًا عند أي تغيّر، بينما `subscribeToRow` تستبدل الصف المحلي كامل بكل realtime echo يوصل بدون أي فحص ترتيب/حداثة. أزرار "سلوك الإرسال" هي أول واجهة بالتطبيق تدعو لتجربة 3 خيارات بسرعة على نفس الحقل، فـechoes متعددة متراكبة ممكن توصل بترتيب غير متسلسل وتخلي القيمة المعروضة تقفز لحظيًا. **الإصلاح:** debounce (~500ms) على نداء الـupsert نفسه بذلك الـeffect — فقط آخر قيمة بعد توقف التفاعل تُكتب فعليًا، فيختفي احتمال تراكب أكثر من upsert/echo على نفس الصف. الحالة المحلية تتحدث فورًا كما هي (التجربة البصرية فورية)، فقط توقيت كتابة قاعدة البيانات تأخر قليلًا. **لو استمر العرض بعد النشر، الخطوة التالية تشخيص مباشر (سطر تشخيص على الشاشة، بنفس منهج §18) بدل تخمين إضافي.**
