# دليل مرجعي — تطبيق رغوة (Laundry POS)

وثيقة تشرح كيف يشتغل كل جزء رئيسي بالتطبيق وظيفياً، بدون تفاصيل تقنية أو أكواد — لأي شخص يبي يفهم آلية عمل النظام.

**ملاحظة عامة:** كل شاشات التطبيق (الكاشير، العملاء، الموردين، التقارير، لوحة الأدمن) موجودة بملف واحد اسمه `App.jsx` — ما فيه ملفات منفصلة لكل صفحة.

**آخر تحديث لهذا الملف:** 2026-08-24. **لأي جلسة جديدة:** اكتب "اقرأ ملف PROJECT_REFERENCE.md وطبّق كل السياق منه" وبكون عارف كل شي مكتوب هنا مباشرة بدون ما تحتاج تشرح من جديد.

---

## 1) البحث عن العميل بشاشة البيع (POS)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `customers` (Supabase/Postgres) — كل صف فيه عمود `tenant_id` يربطه بالمحل

الكاشير يكتب بمربع بحث، والنظام يطابق فوراً (بدون ضغط زر) مع: اسم العميل (جزء من الاسم، بدون حساسية لحالة الأحرف)، أو رقم الجوال (جزء منه)، أو الرقم التعريفي للعميل. لو كتب علامة `#` قبل الأرقام، البحث يصير حصرياً بالرقم التعريفي فقط. النتائج تظهر بقائمة منسدلة حية أثناء الكتابة، والضغط على نتيجة يربط ذلك العميل بالفاتورة الحالية.

---

## 2) شاشة Active Invoices (الفواتير النشطة)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `invoices` (Supabase/Postgres)، مفلترة بعمود `tenant_id`

كل فاتورة تُنشأ من الكاشير تبدأ "مفتوحة" (غير مغلقة) وتظهر بهذي الشاشة تلقائياً، طالما إنها **مو** طلب توصيل. حالة الفاتورة المعروضة (استلام ← غسيل ← كوي ← جاهز ← تم التسليم) هي **أقل مرحلة وصلت لها أي قطعة بداخلها** — يعني لو قطعة وحدة لسا بمرحلة "غسيل" وباقي القطع "جاهزة"، الفاتورة كلها تظهر "غسيل". الفاتورة تختفي من هذي الشاشة فقط لما الموظف يضغط يدوياً زر "إغلاق الكل" من داخلها.

---

## 3) شاشة Active Delivery Invoices (فواتير التوصيل النشطة)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `invoices` (نفس مصدر الفواتير العادي)

نفس آلية شاشة "الفواتير النشطة" بالضبط، لكن تعرض فقط الفواتير اللي تم تحديدها كـ"طلب توصيل" وقت البيع (وفيها رسوم توصيل). فاتورة التوصيل تختفي من هذي القائمة بنفس الطريقة: بضغط "إغلاق الكل" يدوياً من داخلها.

---

## 4) سجل العملاء (Customer Ledger) ونظام المحفظة/الديون

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `customers` (الأرصدة الحالية) + جدول `customer_transactions` (سجل تاريخي لكل عملية شحن/سداد)

لكل عميل رصيدان منفصلان: **رصيد محفظة** (مبلغ مدفوع مسبقاً يقدر يصرفه لاحقاً) و**دين** (مبلغ مستحق عليه للمحل). شحن المحفظة يزيد الرصيد (ممكن مع خصم)، ولو دُفع بمبلغ حقيقي (نقد/شبكة) يُصدر له فاتورة/إيصال حقيقي بنفس اللحظة — عشان الإيراد يُحتسب مرة وحدة بس، مو مرتين لما يصرف من محفظته لاحقاً. سداد الدين الآن يمنع نهائياً إدخال مبلغ أكبر من المستحق فعلياً — النظام يرفض العملية ويعرض تحذير بدل ما يقبل أي مبلغ بصمت.

---

## 5) الموردين والمشتريات وحماية حد الدفعة

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `suppliers` (بيانات المورد ورصيده) + جدول `purchases` (كل عملية شراء)

كل مورد له رصيد يمثل المبلغ المستحق عليه للمحل (يزيد فقط لو الشراء كان "آجل"، ما يتأثر لو كان "نقد"). عند تسجيل سداد لهذا الرصيد، النظام يرفض أي مبلغ أكبر من المستحق فعلياً ويعرض رسالة تحذير واضحة، فما يصير رصيد المورد سالب أبداً بالغلط. كمان، ممكن إرفاق صورة أو ملف فاتورة المورد الأصلية مع كل عملية شراء للرجوع لها لاحقاً.

---

## 6) العروض والخصومات ومنع التداخل

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جدول `promotions`

كل خصم له نوع (نسبة % أو مبلغ ثابت)، وإما يُطبّق تلقائياً أو يتطلب كوبون، وله فترة سريان اختيارية (بداية/نهاية). أي خصم موجود يمكن تعديله أو إلغاؤه (الإلغاء يعطّله فقط، ما يحذفه نهائياً). عند إضافة أو تعديل خصم، النظام يتحقق من كل خصم آخر لسا نشط (غير ملغى) ويرفض الحفظ لو فترته الزمنية تتقاطع مع خصم موجود، حتى ما يصير التباس أي خصم يُطبّق عند البيع.

---

## 7) التقارير (كل تبويب)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** مزيج من `invoices` + `purchases` + `expenses` + `customers` + `suppliers`

- **Sales Ledger (سجل المبيعات):** قائمة فواتير البيع قابلة للفلترة بتاريخ وطريقة دفع، مع تقسيم كل مبلغ لصافي + ضريبة + إجمالي.
- **Procurement Ledger (سجل المشتريات):** قائمة كل عمليات الشراء من الموردين مع إجمالي المصروف، وضريبة المدخلات المحتسبة فقط من الموردين اللي عندهم رقم ضريبي مسجّل.
- **Expenses (المصروفات):** قائمة مصاريف المحل العامة، كل مصروف عليه علامة "شامل الضريبة" أو "معفى"، قابلة للفلترة بتاريخ.
- **Profit & Loss (الربح والخسارة):** الإيرادات ناقص (المشتريات + المصروفات)، معروضة "منذ افتتاح المحل" وأيضاً لفترة محددة يختارها المستخدم، مع هامش الربح كنسبة.
- **Tax Return (الإقرار الضريبي):** ملخص ضريبة القيمة المضافة على طريقة هيئة الزكاة والضريبة (ربع سنوي أو شهري) — ضريبة المخرجات من المبيعات وضريبة المدخلات من المشتريات (فقط من الموردين المسجّلين ضريبياً)، وينتهي بصافي مبلغ مستحق للهيئة أو قابل للاسترداد، قابل للتصدير CSV أو الطباعة.

---

## 8) لوحة تحكم الأدمن

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** جداول `registration_requests` + `tenants` + `sales_inquiries` + `platform_config`

- **طلبات التسجيل:** كل محل جديد يسجل حساب يُنشئ طلب ينتظر موافقة الأدمن (أو يُقبل تلقائياً حسب إعداد النظام)؛ الموافقة تُنشئ سجل المحل الفعلي.
- **إدارة بيانات المحلات:** يسمح للأدمن يشوف (قراءة فقط) فواتير/منتجات/مصاريف/مشتريات أي محل، ويتحكم بطرق الدفع المتاحة لذلك المحل.
- **التقارير المجمّعة:** أرقام تجمع بيانات كل المحلات مباشرة وبشكل حي — أفضل المحلات من ناحية الإيرادات، الأكثر نشاطاً بعدد الفواتير، نمو التسجيلات الجديدة بمرور الوقت، وتوزيع حالة سداد الفواتير عبر المنصة.
- **حذف محل:** يحذف نهائياً سجل المحل وكل ما تحته (منتجات، فواتير، عملاء، إلخ) وطلب التسجيل الأصلي المرتبط فيه، بعد نافذة تأكيد — عملية لا رجعة فيها.

---

## 9) نظام الفاتورة الضريبية المبسطة (QR)

- **📁 الملف:** `App.jsx`
- **🗄️ مصدر البيانات:** لا يُخزَّن بشكل منفصل — يُبنى وقت الطباعة من بيانات الفاتورة المكتملة نفسها

رمز QR (بصيغة هيئة الزكاة والضريبة السعودية) يظهر بالطباعة **فقط** لما تكون العملية بيع خاضع للضريبة فعلياً — يعني ما دُفع من محفظة العميل، والمحل عنده رقم ضريبي مسجّل بالإعدادات. غير كذا، تُطبع إيصال عادي بدون QR. الرمز يشفّر اسم المحل، الرقم الضريبي، تاريخ ووقت البيع، والإجمالي ومبلغ الضريبة — بنفس القيم المطبوعة بالضبط.

---

## 10) هيكل قاعدة البيانات العام (Supabase / Postgres)

النظام يستخدم Supabase (قاعدة بيانات Postgres حقيقية) — **مو Firestore**. لا يوجد تداخل "مستند ← مستندات فرعية"، كل الجداول مسطّحة (flat)، وكل جدول خاص ببيانات المحلات فيه عمود `tenant_id` يربط كل صف بمحله، مع سياسات Row Level Security (RLS) على مستوى قاعدة البيانات تمنع أي محل من قراءة أو تعديل صفوف محل ثاني.

- **`tenants`** — صف واحد لكل محل معتمد، ومعرّفه (`id`) هو نفسه حساب دخول صاحب المحل (Supabase Auth UID). الجداول التالية كلها فيها عمود `tenant_id` يشاور على هذا الصف:
  - `products`, `categories` — المنتجات وتصنيفاتها.
  - `invoices` — كل عملية بيع (وكل عملية شحن محفظة بمبلغ حقيقي).
  - `customers`, `customer_transactions` — بيانات العملاء وسجل محافظهم وديونهم.
  - `suppliers`, `purchases` — الموردين والمبالغ المدفوعة لهم.
  - `expense_categories`, `expenses` — المصاريف العامة.
  - `addons`, `service_types` — الإضافات الاختيارية وأنواع الخدمة.
  - `promotions` — الخصومات والعروض.
  - `tenant_settings` — بيانات المحل، كلمة مرور صاحب المحل (مخزّنة كـ hash)، طرق الدفع المفعّلة، لغة الواجهة.
- **`registration_requests`** — صف مستقل لكل طلب تسجيل محل جديد، مرتبط بحساب الدخول عبر عمود `uid` لكن **منفصل تماماً** عن `tenants` (حذف محل من `tenants` ما يحذف طلبه تلقائياً — لازم حذف الاثنين).
- **`sales_inquiries`** — رسائل نموذج "تواصل مع المبيعات" بصفحة الهبوط العامة، غير مرتبطة بأي محل.
- **`platform_config`** — جدول بصف واحد مشترك يحدد من هم الأدمنز (`admin_emails`)، وهل التسجيلات الجديدة تُقبل تلقائياً أو لأ (`auto_approve`).
- **`platform_admins`** — جدول صلاحيات المدير الفعلي (منفصل عن قائمة `admin_emails` النصية) — عضوية هذا الجدول هي اللي فعلياً تفعّل دالة `is_admin()` بقاعدة البيانات وتمنح وصول تجاوز RLS. `admin_emails` بـ`platform_config` مجرد قائمة "مسموحين يدخلوا لوحة الأدمن بالواجهة" — التحقق الحقيقي والحاسم يصير عبر `platform_admins` + `is_admin()`.
- **RLS (Row Level Security):** كل الـ21 جدول بقاعدة البيانات عليهم RLS مفعّل، وتم التأكد المباشر (بتاريخ 2026-08-23) إن كل سياسات `SELECT/INSERT/UPDATE/DELETE` على الجداول المرتبطة بمحل تتبع نمط `tenant_id = auth.uid()` بشكل صحيح — يعني ما فيه ثغرة IDOR حقيقية بقاعدة البيانات. كان فيه سياسات قديمة زايدة على 8 جداول (`categories, customers, expense_categories, expenses, invoices, products, purchases, tenant_settings`) تعطي وصول أدمن عبر بريد إلكتروني مكتوب مباشرة بالسياسة (`ragwapos@gmail.com`) بدل الاعتماد على `is_admin()`/`platform_admins` — تم حذفها (ملف `supabase-audit-drop-legacy-admin-policies.sql`) لأنها كانت تعني إن سحب صلاحية أدمن عبر الواجهة ما يسحبها فعلياً من هذي السياسات القديمة.

---

## 11) الهوية البصرية ونمط التصميم (Design System)

- **الخطوط:** كل التطبيق (مو بس صفحة الهبوط) يستخدم خط Cairo عبر كلاسات `.f-display` / `.f-body` / `.f-mono` (مُعرّفة بمكوّن `Fonts` بالكود).
- **ألوان التصميم (`tailwind.config.js`):** `brand-{50..900}` (تركوازي، اللون الأساسي)، `navy-{600..950}` (خلفيات داكنة، الشريط الجانبي)، `app-{bg,surface,border,border-strong,text,text-muted,text-subtle}` (رمادي فاتح للواجهة الداخلية)، `success/warning/danger/info-{50,500,600,700}` (ألوان الحالة)، ألوان `landing-*` منفصلة لصفحة الهبوط العامة فقط (خلفية فاتحة `#f6faf9`، مو نفس نظام الداخلية الداكن).
- **مكونات بصرية متكررة (مطبّقة بكل الشاشات الداخلية):**
  - **بطاقة KPI:** إطار دائري خفيف + أيقونة بمربع ملوّن + رقم كبير تحته.
  - **شارة حالة/تصنيف:** كبسولة بنقطة صغيرة، لون حسب الحالة (محايد/أساسي/معلومة/تحذير/نجاح/خطر).
  - **جدول:** إطار دائري، رأس الجدول بخلفية رمادية فاتحة، صفوف تتلوّن عند المرور بالماوس.
  - **رقاقات فلترة (Filter chips):** كبسولات، النشط منها تعبئة كاملة — أسود كحلي (`navy-900`) بشاشات الفواتير/المنتجات/التقارير، لكن تركوازي (`brand-500/600`) بفلاتر فئات المنتجات داخل نقطة البيع تحديداً (اختلاف مقصود، مو خطأ).
- **الشريط الجانبي:** العنصر النشط له تعبئة تركوازية شفافة + إطار داخلي رفيع + نقطة مؤشر، غير النشط نص رمادي فاتح فقط.
- **زر التبديل (Toggle) المشترك:** ثابت بالتصميم (`teal-600`/`stone-300`) — تم إصلاح بق فيه الدائرة الداخلية تطلع خارج حدود الزر بسبب اتجاه RTL (الحل: فرض `dir="ltr"` داخلياً على مسار الزر نفسه فقط).

---

## 12) نقطة البيع (POS) — تفاصيل الشاشة بعد آخر تعديل

- شريط "استلام من الفرع / توصيل" اللي كان أعلى الشاشة **انحذف بالكامل**. التوصيل الحين مربّع صغير داخل شبكة طرق الدفع نفسها، جنب "دفع متعدد" — تفعيله يفتح خانة سعر التوصيل تلقائياً، وما يأثر على اختيار طريقة الدفع الفعلية إطلاقاً (تبقى قابلة للاختيار بشكل طبيعي).
- خانة "كود الخصم" منقولة فوق، جنب مربع التوصيل مباشرة.
- طرق الدفع الحقيقية بنقطة البيع: نقدًا، شبكة خارجية، رصيد المحفظة، آجل (على الحساب) — بالإضافة لخيار خامس خاص بالكاشير فقط: "دفع متعدد" (Split)، وهذا الخيار الخامس **ما يظهر أبداً** كطريقة دفع قابلة للتفعيل/الإيقاف بشاشة الإعدادات (لأنه سلوك عملية بيع، مو طريقة دفع مستقلة).

---

## 13) الإعدادات — قسم "صلاحيات المالك فقط"

كرت "طرق الدفع المفعّلة" وكرت "إغلاق الأقسام عن الموظفين" ظاهرين دايماً مباشرة بالصفحة (مو مخفيين خلف بطاقة "افتح القسم" لازم تدخلها أول). الحماية الفعلية: أي محاولة تبديل زر جوه أي من الكرتين تطلب كلمة مرور المالك قبل ما تنطبق فعلياً — يعني الحماية موجودة على مستوى الفعل نفسه، مو على مستوى رؤية الكرت.

الأقسام الخمسة القابلة للقفل الفردي بكلمة مرور خاصة بها (`OWNER_SECTIONS`): سجل العملاء، المنتجات، المشتريات والمصروفات، العروض، التقارير. "طرق الدفع" **مو** قسم سادس قابل للقفل الفردي — هي محمية أصلاً بنفس كلمة مرور المالك الرئيسية اللي تحمي الكرتين كلهم، وتظهر بقائمة الأقسام كصف ثابت بعلامة "محمي" فقط للتناسق البصري.

---

## 14) الفوترة الإلكترونية (ZATCA Phase 2) — سكافولد قيد التحضير

ملفات `api/zatca-generate-keys.js`, `api/zatca-request-compliance-csid.js`, `api/zatca-sign-invoice.js` تبني تدفق فوترة إلكترونية متوافق مع هيئة الزكاة والضريبة (المرحلة الثانية):
- توليد مفتاح خاص (ECDSA secp256k1) وتشفيره (AES-GCM) قبل التخزين بجدول `zatca_configurations` — المفتاح المفكوك ما يطلع من السيرفر أبداً.
- بناء فاتورة UBL 2.1 (XML) تتضمن بيانات الفاتورة، توقيعها، وبناء رمز QR (Phase 2: 9 حقول، تشمل التوقيع والمفتاح العام).
- **الحالة الحالية:** سكافولد — تدفق "الاتصال/الاختبار" مع هيئة الزكاة **محاكى** (مو اتصال حقيقي فعلي بعد)؛ جزء بناء XML وحساب الهاش والتوقيع حقيقي وصحيح تقنياً، لكن بلوك التوقيع الرقمي (XAdES) ما تم التحقق منه بعد مقابل استجابة حقيقية من الهيئة. آخر إصلاح أمني (2026-08-24): تم التأكد إن كل القيم المدرجة بالـXML (خصوصاً `it.qty` وتاريخ/وقت الفاتورة) تمر عبر دالة `esc()` لمنع حقن XML — كانت فيها ثغرة حقيقية صغيرة تم إصلاحها.

---

## 15) الوضع الأمني الحالي (آخر تدقيق: 2026-08-24)

- **رؤوس الحماية:** `vercel.json` يضيف CSP، HSTS، X-Frame-Options: DENY، X-Content-Type-Options: nosniff، Referrer-Policy، Permissions-Policy على كل الاستجابات، بالإضافة لإعادة توجيه أي مسار غير معروف لـ`index.html` (حماية لأي رابط عميق مستقبلي).
- **صندوق تخزين الملفات:** الـbuckets `invoice-pdfs` و`tenant-documents` عليهم الحين حد فعلي من جهة السيرفر لحجم الملف (5 ميجا) ونوعه (صور + PDF فقط) — قبل كذا كان الفحص الوحيد بالمتصفح فقط (قابل للتلاعب).
- **شاشة خطأ:** التطبيق ملفوف بـ`Sentry.ErrorBoundary` — أي خطأ برمجي غير متوقع يعرض شاشة "حدث خطأ، أعد التحميل" بدل شاشة بيضاء فاضية.
- **فحص أسرار قبل كل commit:** ملف `.githooks/pre-commit` (مفعّل تلقائياً لأي نسخة من المشروع عبر `npm install` بفضل سكربت `prepare` بـ`package.json`) يفحص كل commit جديد عن أنماط أسرار حقيقية (مفاتيح خاصة، مفاتيح AWS/Stripe/Google، JWT خام، إلخ) ويرفض الـcommit لو لقى شي مشبوه.
- **إعدادات Cloudflare (نطاق ragwapos.com):**
  - DMARC: `p=quarantine` (كان `p=none`) — الخطوة القادمة (قرار بشري، مو تلقائي): الانتقال لـ`p=reject` بعد مراجعة تقارير الـquarantine.
  - أقل إصدار TLS مسموح: **1.2** (كان 1.0).
  - سجلات CAA مضافة صراحة: `pki.goog` (Google Trust Services، الشهادة الأساسية) و`letsencrypt.org` (Let's Encrypt، الشهادة الاحتياطية).
  - **DNSSEC مفعّل ونشط** (تأكيد Cloudflare: "Success! ragwapos.com is protected with DNSSEC").
- **فحص أمني خارجي (Raqib):** المشروع متصل بخدمة فحص أمني اسمها Raqib. تم إعداد Raqib كـMCP server باسم `raqib` (نطاق محلي/خاص بهذا المشروع فقط، يحتاج إعادة تشغيل الجلسة عشان تظهر أدواته `trigger_scan`/`get_report`) — الهدف منه تشغيل فحوصات أمنية مستقبلية مباشرة من جوه الجلسة بدل النسخ اليدوي للنتائج.

---

## 16) ملخص تاريخي مختصر لمراحل العمل الأخيرة

1. **إعادة تصميم شاملة (Phases 1-4):** نقطة البيع، الفواتير، سجل العملاء، الإعدادات — نُشرت وأصبحت مباشرة على الموقع.
2. **مرحلة تصحيح دقيق (Strict pixel-correction):** تعديل 8 شاشات متوازي عبر وكلاء مستقلين (git worktrees) بمرجعية تصميم محددة بالحرف، دُمجت كلها بنجاح بفرع واحد بدون تعارضات متبقية.
3. **تنظيف كود ميت:** حذف أيقونات/متغيرات ترجمة غير مستخدمة، حذف إيموجي متبقي (استبدال بأيقونات Lucide حقيقية)، تأكيد تطابق 562 مفتاح ترجمة بالضبط بين العربي/الإنجليزي/الأردو بدون تكرار.
4. **تعديلات واجهة إضافية (بناءً على طلبات محددة):** إعادة تصميم قسم "صلاحيات المالك فقط" بالإعدادات، إعادة تنظيم شريط التوصيل بنقطة البيع، توزيع أعمدة جداول الفواتير بالتساوي، إصلاح موضع خانة البحث بالمنتجات، توحيد عرض خانة البحث بالمشتريات مع المصروفات، إصلاح بق زر التبديل.
5. **تدقيق أمني شامل (Security & Production-Readiness Audit):** فحص كامل — أسرار، مصادقة، RLS/IDOR، CORS، رؤوس الحماية، رفع الملفات — طُبّق كل إصلاح مُتحقق منه مباشرة (مو بس نظري)، شمل الاتصال المباشر بقاعدة بيانات Supabase الحية للتأكد من حالة RLS الفعلية بدل الافتراض.
6. **معالجة نتائج فحص Raqib (12 نتيجة):** تبيّن إن أغلب النتائج "الحرجة" كانت إنذارات كاذبة بعد التحقق الفعلي (مفاتيح عامة مصممة تكون ظاهرة، CORS محمي أصلاً بقائمة مسموحة)، لكن لقينا وأصلحنا ثغرة حقن XML حقيقية بملف توقيع فواتير ZATCA، وأضفنا رابط سياسة خصوصية، وشددنا كل إعدادات DNS/TLS بـCloudflare (DMARC/TLS/CAA/DNSSEC) بشكل حقيقي متحقق منه عبر استعلامات DNS عامة.
